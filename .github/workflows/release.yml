# ============================================================================
# COFIRA - Release Management
# ============================================================================
# Automated release process with semantic versioning
# Runs: On push to main with version tags
# Features:
#   - Semantic versioning
#   - Automated changelog
#   - GitHub release creation
#   - Docker image tagging
#   - Release notes generation
# ============================================================================

name: 🚀 Release

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:
    inputs:
      version:
        description: "Release version (e.g., 1.0.0)"
        required: true

permissions:
  contents: write
  packages: write
  pull-requests: write

jobs:
  # ==========================================================================
  # Create Release
  # ==========================================================================
  release:
    name: 📦 Create Release
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      changelog: ${{ steps.changelog.outputs.changelog }}
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🏷️ Get version
        id: version
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            VERSION="${{ github.event.inputs.version }}"
          else
            VERSION=${GITHUB_REF#refs/tags/v}
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "## 📦 Release v$VERSION" >> $GITHUB_STEP_SUMMARY

      - name: 📝 Generate changelog
        id: changelog
        run: |
          PREVIOUS_TAG=$(git describe --abbrev=0 --tags $(git rev-list --tags --skip=1 --max-count=1) 2>/dev/null || echo "")

          cat > RELEASE_NOTES.md << 'EOF'
          # 🚀 Release v${{ steps.version.outputs.version }}

          ## 📋 What's Changed

          EOF

          if [ -n "$PREVIOUS_TAG" ]; then
            git log $PREVIOUS_TAG..HEAD --pretty=format:"- %s (@%an)" >> RELEASE_NOTES.md
          else
            git log --pretty=format:"- %s (@%an)" >> RELEASE_NOTES.md
          fi

          cat >> RELEASE_NOTES.md << 'EOF'

          ## 🐳 Docker Images

          ### Frontend
          \`\`\`bash
          docker pull ghcr.io/${{ github.repository }}/cofira-frontend:v${{ steps.version.outputs.version }}
          \`\`\`

          ### Backend
          \`\`\`bash
          docker pull ghcr.io/${{ github.repository }}/cofira-backend:v${{ steps.version.outputs.version }}
          \`\`\`

          ## 📚 Documentation

          - [Installation Guide](./INSTALLATION.md)
          - [API Documentation](./API-DOCS.md)
          - [User Guide](./USER_GUIDE.md)

          ## 🙏 Contributors

          Thank you to all contributors who made this release possible!
          EOF

          cat RELEASE_NOTES.md >> $GITHUB_STEP_SUMMARY

      - name: 🏷️ Create GitHub Release
        uses: ncipollo/release-action@v1
        with:
          tag: v${{ steps.version.outputs.version }}
          name: Release v${{ steps.version.outputs.version }}
          bodyFile: RELEASE_NOTES.md
          draft: false
          prerelease: false
          token: ${{ secrets.GITHUB_TOKEN }}
          generateReleaseNotes: true

  # ==========================================================================
  # Build Release Artifacts
  # ==========================================================================
  build-artifacts:
    name: 🏗️ Build Release Artifacts
    runs-on: ubuntu-latest
    needs: release
    strategy:
      matrix:
        include:
          - component: frontend
            path: cofira-app
          - component: backend
            path: backend
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🏗️ Build ${{ matrix.component }}
        run: |
          echo "Building ${{ matrix.component }}..."

      - name: 📦 Create archive
        run: |
          tar -czf ${{ matrix.component }}-v${{ needs.release.outputs.version }}.tar.gz ${{ matrix.path }}

      - name: 📤 Upload to release
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: ${{ matrix.component }}-v${{ needs.release.outputs.version }}.tar.gz
          tag: v${{ needs.release.outputs.version }}
          overwrite: true

  # ==========================================================================
  # Tag Summary
  # ==========================================================================
  tag-summary:
    name: 🏷️ Tag Summary
    runs-on: ubuntu-latest
    needs: release
    steps:
      - name: 📊 Docker Tag Information
        run: |
          echo "## 🏷️ Docker Image Tags" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Frontend Image" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`bash" >> $GITHUB_STEP_SUMMARY
          echo "docker pull ghcr.io/${{ github.repository }}/cofira-frontend:v${{ needs.release.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Backend Image" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`bash" >> $GITHUB_STEP_SUMMARY
          echo "docker pull ghcr.io/${{ github.repository }}/cofira-backend:v${{ needs.release.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "ℹ️ Docker images are built and tagged by CI/CD pipelines" >> $GITHUB_STEP_SUMMARY

  # ==========================================================================
  # Update Documentation
  # ==========================================================================
  update-docs:
    name: 📚 Update Documentation
    runs-on: ubuntu-latest
    needs: release
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 📝 Update version in README
        run: |
          sed -i 's/Version: [0-9]\+\.[0-9]\+\.[0-9]\+/Version: ${{ needs.release.outputs.version }}/' README.md || true

      - name: 📝 Update CHANGELOG
        run: |
          cat > TEMP_CHANGELOG.md << EOF
          # Changelog

          ## [v${{ needs.release.outputs.version }}] - $(date +%Y-%m-%d)

          ${{ needs.release.outputs.changelog }}

          EOF

          if [ -f CHANGELOG.md ]; then
            tail -n +2 CHANGELOG.md >> TEMP_CHANGELOG.md
          fi

          mv TEMP_CHANGELOG.md CHANGELOG.md

      - name: 💾 Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md CHANGELOG.md
          git commit -m "📝 Update documentation for v${{ needs.release.outputs.version }}" || true
          git push || true

  # ==========================================================================
  # Summary
  # ==========================================================================
  summary:
    name: 📊 Release Summary
    runs-on: ubuntu-latest
    needs: [release, build-artifacts, tag-summary, update-docs]
    if: always()
    steps:
      - name: 🎉 Success message
        run: |
          echo "# 🎉 Release v${{ needs.release.outputs.version }} Complete!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## ✅ Completed Tasks" >> $GITHUB_STEP_SUMMARY
          echo "- Release: ${{ needs.release.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- Build Artifacts: ${{ needs.build-artifacts.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- Tag Summary: ${{ needs.tag-summary.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- Documentation: ${{ needs.update-docs.result }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "🔗 [View Release](https://github.com/${{ github.repository }}/releases/tag/v${{ needs.release.outputs.version }})" >> $GITHUB_STEP_SUMMARY
